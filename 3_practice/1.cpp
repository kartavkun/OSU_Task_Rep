#include <iostream>
#include <cmath>

using namespace std;

int main() {
    for(float x = 1; x <= 5; x += 0.5) {
        cout << "f("<< i << ") = " << log(i) + 1 << endl; 
    }
    return 0;
}
